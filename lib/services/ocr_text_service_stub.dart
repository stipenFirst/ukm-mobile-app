class OcrTextService {
  Future<String> recognizeTextFromImagePath(String imagePath) async {
    throw UnsupportedError(
      'Fitur scan KTM belum didukung saat aplikasi dijalankan di Chrome/Web. Silakan tes fitur scan KTM di HP Android.',
    );
  }
}
